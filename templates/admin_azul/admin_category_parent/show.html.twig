{% extends 'base.html.twig' %}

{% block title %}CategoryParent{% endblock %}

{% block body %}
    <h1>CategoryParent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_parent.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_parent.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_parent.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ category_parent.image }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ category_parent.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_parent_index') }}">back to list</a>

    <a href="{{ path('category_parent_edit', {'id': category_parent.id}) }}">edit</a>

    {{ include('category_parent/_delete_form.html.twig') }}
{% endblock %}
