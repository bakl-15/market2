<form method="post" action="{{ path('category_parent_delete', {'id': category_parent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_parent.id) }}">
    <button class="btn">Delete</button>
</form>
