{% extends 'base.html.twig' %}

{% block title %}HomeSlider{% endblock %}

{% block body %}
    <h1>HomeSlider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_slider.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ home_slider.title }}</td>
            </tr>
            <tr>
                <th>HeaderLeft</th>
                <td>{{ home_slider.HeaderLeft }}</td>
            </tr>
            <tr>
                <th>HeaderRight</th>
                <td>{{ home_slider.headerRight }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ home_slider.description }}</td>
            </tr>
            <tr>
                <th>ButtonMessage</th>
                <td>{{ home_slider.buttonMessage }}</td>
            </tr>
            <tr>
                <th>ButtonUrl</th>
                <td>{{ home_slider.buttonUrl }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ home_slider.image }}</td>
            </tr>
            <tr>
                <th>IsDisplayed</th>
                <td>{{ home_slider.isDisplayed ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_slider_index') }}">back to list</a>

    <a href="{{ path('home_slider_edit', {'id': home_slider.id}) }}">edit</a>

    {{ include('home_slider/_delete_form.html.twig') }}
{% endblock %}
