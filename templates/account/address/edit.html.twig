{% extends 'base.html.twig' %}

{% block title %}Edit Address{% endblock %}

{% block body %}
      {{include("./partials/breadcrumb.html.twig", {data: { ele1 :'profile', ele2 : 'Editer l\' addresses'}}) }}
       <div class="body-content outer-top-xs" id="top-banner-and-menu">
           <div class="container">
                <div class="row">
		            <div class="col-xs-12 col-sm-12 col-md-3 sidebar">
                        {% include "./partials/sidebar.html.twig" %}
                    </div>
                    <div class="col-xs-12 col-sm-12 col-md-9">
                        <div class="terms-conditions-page">
                           <h3>Modifier une Adresse <strong> {{ address.fullname}} </strong></h3>
                           <hr>
                             {{ include('account/address/_form.html.twig', {'button_label': ' Editer'}) }}
                            <div class="form-group mt-5">
                                  <a href="{{ path('address_index') }}"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
                            </div>
                            
                       </div> 
                    </div>
                </div>
            </div>
        </div>
    
       {{ include('account/address/_delete_form.html.twig') }}
{% endblock %}
